dist/
.astro/
node_modules/
src/generated/
pnpm-lock.yaml

# Contract documents, authored outside this repo's formatting rules.
# BRAND.md §intro: where code and these disagree, they win — so we do not
# rewrite them to satisfy a formatter.
docs/SPEC.md
docs/BRAND.md
docs/AGENT.md

# Portfolio intake output — written by an agent in another repository and copied
# in as source material for the admin form (docs/portfolio-intake-prompt.md).
# Same reasoning as the contract documents above: it is not this repo's prose to
# reformat, and a formatter rewriting it would obscure what the agent produced.
projects/
